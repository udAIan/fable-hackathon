# Public client config (VITE_* is baked into the bundle — never put secrets here).
# Committed as the local-dev default. In production, Cloudflare Pages' build-time
# VITE_API_URL env var takes priority over this file.
VITE_API_URL=https://server-production-311e2.up.railway.app
