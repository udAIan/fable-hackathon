# Studio Sandbox Project

You are editing a Vite + React + TypeScript app in `/home/user/project`.

- Build the UI the user asks for by editing files under `src/`.
- Keep changes focused and the app runnable.
- The dev server runs on port 5175 (`pnpm dev`).
- Prefer small, composable components.
- Do not commit changes.
